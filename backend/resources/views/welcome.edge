<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>GAD JM - Consultas por rango</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  @entryPointStyles('app')
  @entryPointScripts('app')
</head>
<body>
  <div class="container">
    <div class="pt-5 pb-5">
      <h1 class="title">Consulta de código por rango</h1>
      <p class="subtitle">
        A continuación introduzca una función JavaScript, un rango de búsqueda y ejecute su consulta.
      </p>

      <form>
        <div class="row">
          <div class="col-8">
            <label for="code" class="form-label">Código</label>
            <textarea name="code" id="code" class="form-control" style="height: 400px">{{ code }}</textarea>
          </div>
          <div class="col-4">
            <label class="form-label">Vector característico</label>
            <pre class="card p-2" style="color: #d63384;"><code>{{ JSON.stringify(vector, null, 2) }}</code></pre>
          </div>
        </div>
        <div class="row mt-2">
          <div class="col">
            <label for="range" class="form-label">Rango de búsqueda</label>
            <input type="number" min="0" name="range" id="range" class="form-control" value="{{ range }}">
          </div>
          <div class="col">
            <label for="limit" class="form-label">Límite de resultados</label>
            <input type="number" min="1" name="limit" id="limit" class="form-control" value="{{ limit }}">
          </div>
        </div>
        <div class="btn-group mt-2 w-100" role="group">
          <button type="reset" class="btn btn-danger" onclick="window.location = '/'">Reiniciar</button>
          <button type="submit" class="btn btn-primary">Consultar</button>
        </div>
      </form>

      @if(showResults)
      <div class="mt-3">
        <h3>Resultados de la consulta ({{ results.length }})</h3>

        <div class="mt-2">
          @each(result in results)
          <div class="card mb-2">
            <div class="card-body">
              <div class="row">
                <div class="col-8">
                  <h5 class="card-title">Distancia: {{ result.distance }}</h5>
                  <pre class="mb-0" style="color: #d63384"><code>{{ result.code }}</code></pre>
                </div>
                <div class="col-4">
                  <h5 class="card-title">Vector característico</h5>
                  <pre class="mb-0" style="color: #d63384"><code>{{ JSON.stringify(vector, null, 2) }}</code></pre>
                </div>
              </div>
            </div>
          </div>
          @end
        </div>
      </div>
      @end
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
</body>
</html>
